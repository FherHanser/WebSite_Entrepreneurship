import { render, screen } from '@testing-library/react';
import App from './App';

test('renders App component', () => {
  render(<App />);
  // Aquí puedes realizar otras aserciones sobre la renderización de tu aplicación
});
